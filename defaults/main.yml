---
terraform_version: '0.9.5'
terraform_install_path: '/usr/local/bin'

# Download URL
terraform_download_url: 'https://releases.hashicorp.com/terraform/{{ terraform_version }}'
terraform_checksum_url: '{{ terraform_download_url }}/terraform_{{ terraform_version }}_SHA256SUMS'
